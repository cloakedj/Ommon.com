import { ShrinkTextPipe } from './shrink-text.pipe';

describe('ShrinkTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ShrinkTextPipe();
    expect(pipe).toBeTruthy();
  });
});
